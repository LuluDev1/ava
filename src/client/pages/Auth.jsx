import React from "react";

export default function Auth() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignContent: "center",
      }}
    >
      Auth
    </div>
  );
}
